(int)$input_id == $id 
 
# php언어에서 문자열을 숫자로 변환시킬때, 문자열이 문자인 경우 0으로 변환된다.
# 위의 코드에서 if id= 문자로 구성된다면 input_id에 0을 입력하면 통과가능
 
$pw = sha1("1"); 
#1의 sha1값은 "356a192b..."
(int)$input_pw == $pw

#sha1은 문자열을 암호화하는 함수 값, 인터넷에서 복호화값을 찾을 수 있음
#php언어에서 문자열을 숫자로 변환할때, 문자가 들어오기전 숫자값까지만 숫자로 변환
#356/////을 입력하면 가능


if((int)$input_id == $id && strlen($input_id) === 10)
if((int)$input_pw == $pw && strlen($input_pw) === 8

